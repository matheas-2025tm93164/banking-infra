apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: account-db-pvc
  namespace: banking-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Secret
metadata:
  name: account-db-secret
  namespace: banking-system
type: Opaque
data:
  POSTGRES_USER: YWNjb3VudF91c2Vy
  POSTGRES_PASSWORD: YWNjb3VudF9wYXNz
  POSTGRES_DB: YWNjb3VudF9kYg==
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-db
  namespace: banking-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: account-db
  template:
    metadata:
      labels:
        app: account-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: account-db-secret
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "account_user", "-d", "account_db"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "account_user", "-d", "account_db"]
            initialDelaySeconds: 15
            periodSeconds: 20
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: account-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: account-db
  namespace: banking-system
spec:
  selector:
    app: account-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
